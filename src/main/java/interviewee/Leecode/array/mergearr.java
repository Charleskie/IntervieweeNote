package interviewee.Leecode.array;

public class mergearr {

    /**
     * 合并两个升序数组到 nums1 中；当前方法体仍是待补全草稿。
     *
     * @param nums1 第一个数组，预留了容纳 nums2 的空间
     * @param m     nums1 中有效元素数量
     * @param nums2 第二个数组
     * @param n     nums2 中有效元素数量
     */
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int p = 0;
        int q = 0;
        if(p < m || q < n){
//            if()if
        }

    }
}
